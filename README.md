
# AI Hacker Assistant

ИИ-агент для обучения и автоматизации этичного пентеста.

### Структура проекта:
```
ai-hacker-assistant/
├── .venv/..
├── core/                     ← Ядро (платформо- и UI-независимое)
│   ├── ai_client.py          ← Работа с ИИ (API, форматы, история чата)
│   ├── command_executor.py   ← Безопасное выполнение команд
│   ├── tools/                ← Модули для nmap, sqlmap, metasploit и т.д.
│   └── config.py             ← Загрузка настроек (API ключ, модель, ОС)
│
├── interfaces/               ← Интерфейсы (можно добавлять новые)
│   ├── cli.py                ← CLI-интерфейс (запуск через терминал)
│   └── gui/                  ← Папка для будущего GUI (Tkinter/PyQt)
│       └── main_window.py
│
├── platforms/                ← Платформо-специфичная логика
│   ├── windows.py            ← Упаковка KLT? Позже. Сейчас — предупреждение.
│   └── kali.py               ← Проверка наличия инструментов (nmap и др.)
│
├── main.py                   ← Точка входа: определяет ОС → запускает нужный интерфейс
├── requirements.txt
└── README.md
```

# 📘 Инструкция по использованию AI Hacker Assistant

## 1. Требования

Для запуска проекта вам понадобится:
- Компьютер с **Windows 10/11** или **Linux** (включая Kali Linux)
- Установленный **Python 3.8 или новее**
- Доступ в интернет (для облачного ИИ) **ИЛИ** локальный ИИ-сервер (например, Ollama)

> 💡 Проект не требует установки специального ПО для пентеста на Windows, но для полной функциональности рекомендуется использовать **Kali Linux** или **WSL2 + Kali**.

---

## 2. Установка

1. Скачайте архив проекта и распакуйте его.
2. Откройте терминал (командную строку) в папке проекта.
3. Установите зависимости:

```bash
pip install -r requirements.txt
```

---

## 3. Настройка облачного ИИ (бесплатно через OpenRouter)

### Шаг 1: Получите API-ключ
1. Перейдите на сайт [https://openrouter.ai](https://openrouter.ai)
2. Войдите через Google, GitHub или электронную почту.
3. Перейдите в раздел **Settings → API Keys**
4. Нажмите **+ Create Key** → скопируйте полученный ключ (начинается с `sk-or-v1-...`)

> 🔐 **Важно**: не публикуйте этот ключ! Он даёт доступ к вашему аккаунту.

### Шаг 2: Настройте конфигурацию
Откройте файл `config.json` и замените содержимое на:

```json
{
  "api_url": "https://openrouter.ai/api/v1",
  "model": "meta-llama/llama-3-70b-instruct",
  "api_key": "sk-or-v1-ваш_ключ_здесь",
  "platform": "auto",
  "language": "ru"
}
```

> ✅ Рекомендуемые бесплатные модели:
> - `meta-llama/llama-3-70b-instruct` — мощная, отлично понимает пентест
> - `mistralai/mixtral-8x7b-instruct` — быстрая и точная
> - Полный список: [https://openrouter.ai/models](https://openrouter.ai/models)

> 💰 OpenRouter даёт **бесплатные кредиты** при регистрации (обычно $1–5), чего хватит на **сотни запросов**.

---

## 4. Запуск

В терминале выполните:

```bash
python main.py
```

Вы увидите сообщение вроде:

```
🔧 Платформа: windows
🧠 Подключено к модели: meta-llama/llama-3-70b-instruct
🚀 AI Hacker Assistant (CLI) запущен.
💡 Совет: спросите 'Как просканировать 192.168.1.1?' или 'Помоги найти уязвимости'
```

Теперь вы можете общаться с ИИ:
- Задавать вопросы по информационной безопасности
- Просить предложить команды для анализа
- Получать пояснения по уязвимостям

> ⚠️ Все команды выполняются **только после вашего подтверждения** — это гарантирует безопасность.

---

## 5. Альтернатива: локальный ИИ (без интернета)

Если вы не хотите использовать облако:
1. Установите [Ollama](https://ollama.com/)
2. Выполните в терминале:
   ```bash
   ollama run llama3
   ```
3. Используйте стандартный `config.json`:
   ```json
   {
     "api_url": "http://localhost:11434/v1",
     "model": "llama3",
     "api_key": null,
     "platform": "auto"
   }
   ```

---

## 6. Важные ограничения

- На **обычной Windows** инструменты вроде `nmap`, `sqlmap`, `gobuster` **не установлены**.  
  → ИИ может предлагать команды, но они не выполнятся.  
  → Решение: используйте **Kali Linux**, **WSL2**, или установите инструменты вручную.
- Проект предназначен **исключительно для обучения и авторизованного тестирования**.
- Все действия требуют **явного согласия пользователя** — автоматической эксплуатации нет.

---
